<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vangiau.example.shoptech">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopTech"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.LichSuMuaHangActivity" />
        <activity android:name=".activity.PaymentDetails" />
        <activity android:name=".activity.PaypalActivity" />
        <activity android:name=".activity.ChiTietDonHangActivity" />
        <activity android:name=".activity.DieuKhoanBaoMatActivity" />
        <activity android:name=".activity.TimKiemActivity" />
        <activity android:name=".activity.NguoiDungActivity" />
        <activity android:name=".activity.TrangChuActivity" />
        <activity android:name=".activity.DangNhapActivity" />
        <activity android:name=".activity.DongHoActivity" />
        <activity android:name=".activity.TabletActivity" />
        <activity android:name=".activity.WebViewActivity" />
        <activity android:name=".activity.TinTucActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activity.ThongTinActivity"
            android:label="@string/title_activity_thong_tin" />
        <activity android:name=".activity.ThanhToanActivity" />
        <activity android:name=".activity.GioHangActivity" />
        <activity android:name=".activity.MacbookActivity" />
        <activity android:name=".activity.ChiTietThietBiActivity" />
        <activity android:name=".activity.TroGiupActivity" />
        <activity android:name=".activity.DienThoaiActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
    </application>

</manifest>